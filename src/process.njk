---
layout: base.njk
title: Process
permalink: /process/index.html
---
<section class="archive">
  <header class="archive__header">
    <h1 class="archive__title">Process</h1>
    <p class="archive__description">Process notes for selected pieces — showing how they came together, including where AI was part of the writing.</p>
  </header>

  {% set currentYear = "" %}
  <div class="archive__posts">
    {%- for post in collections.process -%}
      {% set postYear = post.date | getYear %}
      {% if postYear != currentYear %}
        {% set currentYear = postYear %}
        <h2 class="archive__year">{{ currentYear }}</h2>
      {% endif %}
      <article class="archive__post">
        <time class="archive__date" datetime="{{ post.date | isoDate }}">{{ post.date | formatDate }}</time>
        <a href="{{ post.url }}" class="archive__link">{{ post.data.title }}</a>
      </article>
    {%- endfor -%}
  </div>
</section>
